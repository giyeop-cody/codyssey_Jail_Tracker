{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "Codyssey guild SECOM dashboard and GitHub Pages collector",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "collect": "node ../collect_all.js",
    "check": "node --check server.js && node --check lib/github-sync.js && node --check lib/open-session.js && node --check lib/roster-cache.js && node --check lib/season-week.js && node --check public/app.js && node --check scripts/build-public-history.js",
    "test": "node --test test/*.test.js"
  },
  "keywords": [
    "codyssey",
    "dashboard",
    "github-actions",
    "codespaces"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "libsodium-wrappers": "^0.8.4",
    "node-fetch": "^2.7.0",
    "set-cookie-parser": "^3.1.2"
  }
}
